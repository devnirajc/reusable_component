import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BulkOrderInboxSearchComponent } from './bulk-order-inbox-search.component';

describe('BulkOrderInboxSearchComponent', () => {
  let component: BulkOrderInboxSearchComponent;
  let fixture: ComponentFixture<BulkOrderInboxSearchComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ BulkOrderInboxSearchComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(BulkOrderInboxSearchComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
