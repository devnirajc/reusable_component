import { DomChangeDirective } from './dom-changes.directive';

describe('DomChangeDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new DomChangesDirective();
  //   expect(directive).toBeTruthy();
  // });
});
