import { EditableGridConfig } from '@app/shared/components';
export class EditableGridConfiguration {
    public config: any;
    constructor(config: EditableGridConfig) {
        this.config = config;
    }
}